# 部署环境标识；本项目没有需要注入浏览器的秘密变量。
APP_ENV=production

# 部署脚本用于健康检查。首次部署前替换为真实域名。
DOMAIN=<DOMAIN>
HEALTHCHECK_URL=https://<DOMAIN>/

# 生产发布设置。
PRODUCTION_BRANCH=main
RELEASE_KEEP=5

# 任何进入前端构建产物的变量都对浏览器用户可见，不能在此存放密码、Token、私钥或 API Secret。
